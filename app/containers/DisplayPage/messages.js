/*
 * DisplayPage Messages
 *
 * This contains all the text for the DisplayPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DisplayPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Here are all of your strings!',
  },
});
